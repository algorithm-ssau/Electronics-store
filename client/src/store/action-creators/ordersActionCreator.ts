import { Dispatch } from "redux";

export const fetchOrders = () => {
  return async (dispatch: Dispatch) => {
    try {
      setTimeout(() => {
        // todo
      }, 2000);
    } catch (e) {
      // handle error
    }
  };
};
